## What does this change?

<!-- Describe what changed and why. Link any related issue. -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] If this touches `tag_worker.py`, I added a test case to `scripts/smoke-test.js`
- [ ] If this changes a dict key crossing the JS/Python boundary, I updated `types/domain.d.ts` to match
- [ ] If this is a UI change, I ran the app (`npm start`) and exercised it by hand
